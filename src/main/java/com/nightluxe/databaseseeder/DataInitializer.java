package com.nightluxe.databaseseeder;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;

/*public class DataInitializer implements CommandLineRunner {

    @Autowired
    private CategoryRepository categoryRepository;

    @Override
    public void run(String... args) {
        if (categoryRepository.count() == 0) {
            categoryRepository.save(new Category("Auto", "auto", false));
            categoryRepository.save(new Category("Imobiliare", "imobiliare", false));
            categoryRepository.save(new Category("Servicii", "servicii", false));
            categoryRepository.save(new Category("Escorte", "escorte", true)); [cite: 5, 22, 28]
        }
    }

}
 */
